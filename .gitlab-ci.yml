workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_PIPELINE_SOURCE == "web"
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH

include:
  # Analyse de sécurité statique du code
  - template: Jobs/SAST.latest.gitlab-ci.yml

  # Composants de l'Usine Logicielle
  - component: $CI_SERVER_FQDN/usinelogicielle/public/gitlab-ci-components/sonarqube-component/sonarqube@$COMPONENT_UL_SONARQUBE_VERSION
    inputs:
      stage: sonarqube
      sq_users_admin: $SONAR_MEMBERS
      sonar_project_key: $SONAR_PROJECT_KEY
      sonar_project_name: $SONAR_PROJECT_NAME
      tag_image_scripts: $COMPONENT_UL_SONARQUBE_VERSION

  - component: ${CI_SERVER_FQDN}/usinelogicielle/public/gitlab-ci-components/antivirus-clamav/analyse-clamav@$COMPONENT_UL_ANTIVIRUS_VERSION
    inputs:
      list_to_scan:
        - ${CI_PROJECT_DIR}

stages:
  - init
  # quality
  - test
  - package
  - sonarqube
  - security

# Configuration par défaut
default:
  tags:
    - Usine_Logicielle
  image: ${PUBLIC_REMOTE_REGISTRY_DOCKER}/${IMAGE_PYTHON}
  timeout: 30 minutes
  before_script:
    - export REQUESTS_CA_BUNDLE=${CNES_CERTIFICATE}
    - export SSL_CERT_FILE=${CNES_CERTIFICATE}

# Remplacement de l'ancre YAML par une fonction réutilisable via !reference
.jfrog_helpers:
  restore_config:
    - chmod 755 jfrog
    - export UUID=$(cat uuid.txt)

variables:
  DEBUG: "false"
  PUBLIC_REMOTE_REGISTRY_DOCKER: "${ARTIFACTORY_HOST}/publicremotes-docker"
  IMAGE_PYTHON: "python:3.11.14-bookworm"
  IMAGE_PODMAN: "ubi9/podman:9.5"
  IMAGE_SONAR: "sonarsource/sonar-scanner-cli:4.5"

  # JFrog CLI
  JFROG_CLI_VERSION: "${ULVAR_JFROG_CLI_VERSION}"
  CI: "true"
  JFROG_CLI_HOME_DIR: ".jfrog"
  JFROG_CLI_TEMP_DIR: ".jfrog_tmp"
  JFROG_OS: "jfrog-cli-linux-amd64"
  JFROG_CLI_BUILD_NAME: "${CI_PROJECT_PATH}_${CI_COMMIT_REF_SLUG}_gitlab-ci"
  JFROG_CLI_BUILD_NUMBER: "${CI_PIPELINE_ID}"

  # Registres & Dépôts
  CI_REGISTRY: "https://${UL_ARTIFACTORY_HOST}"
  CI_REGISTRY_USER: "${ARTIFACTORY_USER}"
  CI_REGISTRY_PASSWORD: "${ARTIFACTORY_TOKEN}"
  CI_TEMPLATE_REGISTRY_HOST: "${UL_ARTIFACTORY_HOST}/publicremotes-docker"

  # Python / Pip
  PIP_INDEX_URL: "https://${ARTIFACTORY_USER}:${ARTIFACTORY_TOKEN}@${UL_ARTIFACTORY_HOST}/artifactory/api/pypi/pypi/simple"
  PIP_CACHE_DIR: "${CI_PROJECT_DIR}/.pip-cache/"
  CONDA_ENVS_DIRS: ".conda/envs"

  # Docker Image Details
  TARGET_REPO: "obs2co-docker"
  SUFFIX_DOCKER_IMAGE: "grs"
  DOCKER_IMAGE_NAME: "${UL_ARTIFACTORY_HOST}/${TARGET_REPO}/${SUFFIX_DOCKER_IMAGE}"


# =============================================================================
# STAGE : init
# =============================================================================
python-init:
  stage: init
  image: ${PUBLIC_REMOTE_REGISTRY_DOCKER}/${IMAGE_PYTHON}
  before_script:
    - !reference [default, before_script]
    - curl -f -L -u ${ARTIFACTORY_USER}:${ARTIFACTORY_TOKEN} "https://${UL_ARTIFACTORY_HOST}/artifactory/jfrog-cli-go-remote/v2/2.96.0/jfrog-cli-linux-amd64/jfrog" -o jfrog
    - chmod 755 jfrog
    - export UUID=$(cat /proc/sys/kernel/random/uuid)
    - echo "JF_UUID=${UUID}" >> build.env
    - echo ${UUID} > uuid.txt
  script:
    - pip install virtualenv
    - virtualenv python_env
    - source python_env/bin/activate
    - ./jfrog config add ${UUID} --url=https://${UL_ARTIFACTORY_HOST}/ --access-token=${ARTIFACTORY_TOKEN} --interactive=false
    - ./jfrog pip-config --server-id-resolve=${UUID} --repo-resolve=pypi
    - ./jfrog pip install -r requirements.txt --no-cache-dir --force-reinstall --module grs
    - deactivate
  timeout: 15 minutes
  artifacts:
    paths:
      - jfrog
      - uuid.txt
      - python_env/
    reports:
      dotenv: build.env
    expire_in: 1 day

# =============================================================================
# STAGE : quality
# =============================================================================
# pytest:
#   stage: quality
#   image: ${PUBLIC_REMOTE_REGISTRY_DOCKER}/${IMAGE_PYTHON}
#   needs:
#     - python-init
#   script:
#     - source python_env/bin/activate
#     - PYTHONPATH=. pytest --cov=grs --cov-report=xml:.coverage-reports/coverage.xml --cov-report=term --junitxml=reports/unit_tests-report.xml
#     - deactivate
#   artifacts:
#     paths:
#       - .coverage-reports/coverage.xml
#       - reports/unit_tests-report.xml
#     expire_in: 1 day
#     reports:
#       junit: reports/unit_tests-report.xml

# =============================================================================
# STAGE : test
# =============================================================================
python-tests:
  stage: test
  allow_failure: true
  tags:
    - tpa
  needs:
    - python-init
  before_script:
    - export CONDA_SSL_VERIFY=/etc/pki/tls/certs/ca-bundle.crt
    - export REQUESTS_CA_BUNDLE=/etc/pki/tls/certs/ca-bundle.crt
    - export JOB_UUID=$(cat /proc/sys/kernel/random/uuid)
    - !reference [.jfrog_helpers, restore_config]
    - mkdir -p "${JFROG_CLI_HOME_DIR}/security/certs/"
    - cp ${REQUESTS_CA_BUNDLE} "${JFROG_CLI_HOME_DIR}/security/certs/"
    - ./jfrog config add ${JOB_UUID} --artifactory-url=https://${UL_ARTIFACTORY_HOST}/artifactory --access-token=${ARTIFACTORY_TOKEN} --user=${ARTIFACTORY_USER} --enc-password --interactive=false
    - ./jfrog pip-config --server-id-resolve=${JOB_UUID} --repo-resolve=pypi
    - export MY_JOB_WORKDIR="/work/scratch/data/${USER}/${JOB_UUID}"
    - export REF_DIR=/work/scratch/data/${USER}/OBS2CO/CI/REFS/    
    - mkdir -p ${MY_JOB_WORKDIR}/LOGS ${MY_JOB_WORKDIR}/grs2
    - rsync -av --exclude='.git' --exclude='.jfrog' --exclude='python_env' ./ ${MY_JOB_WORKDIR}/grs2/
    - ssh trex.sis.cnes.fr "mkdir -p ${MY_JOB_WORKDIR}/grs2 ${MY_JOB_WORKDIR}/LOGS"
    - rsync -av --exclude='.git' --exclude='.jfrog' --exclude='python_env' ./ trex.sis.cnes.fr:${MY_JOB_WORKDIR}/grs2/
  script:   
    - |
      ssh trex.sis.cnes.fr "MY_JOB_WORKDIR='${MY_JOB_WORKDIR}' REF_DIR='${REF_DIR}' ARTIFACTORY_TOKEN='${ARTIFACTORY_TOKEN}' ARTIFACTORY_USER='${ARTIFACTORY_USER}' bash -s" << 'EOF'
        set -e
        REMOTE_INIT="${MY_JOB_WORKDIR}/grs2/grs/tests/ci/ci-init_env.slurm"
        REMOTE_TEST="${MY_JOB_WORKDIR}/grs2/grs/tests/ci/ci-func-run.slurm"
        REMOTE_CLEAN="${MY_JOB_WORKDIR}/grs2/grs/tests/ci/ci-cleanup_env.slurm"
        echo "Vérification du dossier de travail : ${MY_JOB_WORKDIR}"
        ls -l "${MY_JOB_WORKDIR}"
        echo "Vérification du fichier slurm :"
        ls -l "${REMOTE_INIT}"
        sub_str=$(sbatch --export=MY_JOB_WORKDIR=${MY_JOB_WORKDIR},ARTIFACTORY_TOKEN=${ARTIFACTORY_TOKEN},ARTIFACTORY_USER=${ARTIFACTORY_USER} -o ${MY_JOB_WORKDIR}/LOGS/grs-init_env.out "${REMOTE_INIT}")
        id_init=${sub_str##* }
        echo "Job init -> ${id_init}"
        sub_str=$(sbatch --depend=afterany:${id_init} --export=MY_JOB_WORKDIR=${MY_JOB_WORKDIR},REF_DIR=${REF_DIR} -o ${MY_JOB_WORKDIR}/LOGS/grs-func-tests.out "${REMOTE_TEST}")
        id_job=${sub_str##* }
        echo "Job tests -> ${id_job}"
        sbatch --wait --depend=afterany:${id_job} --export=MY_JOB_WORKDIR=${MY_JOB_WORKDIR} -o ${MY_JOB_WORKDIR}/LOGS/grs-cleanup.out "${REMOTE_CLEAN}"
      EOF
    - mkdir -p reports
    - scp -r trex.sis.cnes.fr:${MY_JOB_WORKDIR}/reports/* reports/ || echo "Pas de dossier reports"
    - scp trex.sis.cnes.fr:${MY_JOB_WORKDIR}/LOGS/* reports/ || echo "Pas de logs"
    - cat reports/ref_diff* || echo "Pas de fichier diff"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web" || $CI_PIPELINE_SOURCE == "merge_request_event" || $CI_COMMIT_TAG || $CI_COMMIT_BRANCH'
  timeout: 60 minutes
  artifacts:
    untracked: true
    expire_in: 1 day
    reports:
      junit: reports/functionnal_tests-report.xml
  cache:
    key: "coverage-reports"
    paths:
      - reports/cov_report.xml
    policy: push

podman-test:
  stage: package
  image:
    name: ${PUBLIC_REMOTE_REGISTRY_DOCKER}/${IMAGE_PODMAN}
    entrypoint: [""]
  needs:
    - python-tests
  before_script:
    # - !reference [default, before_script]
    # - !reference [.jfrog_helpers, restore_config]
    - mkdir -p certs
    - cp ${CNES_CERTIFICATE} certs/ca-bundle.crt
    - cat ${CNES_CERTIFICATE} >> certs/ca-certificates.crt
    - echo "$HTTP_PROXY" > /tmp/proxy_http
    - echo "$HTTPS_PROXY" > /tmp/proxy_https
    - export VERSION_GRS=$(grep version pyproject.toml | cut -d '"' -f 2)
    - if [ -n "${CI_COMMIT_TAG}" ]; then export VERSION_GRS=${CI_COMMIT_TAG}; fi
    - echo "Version GRS -> ${VERSION_GRS}"
  script:
    - echo "https://${ARTIFACTORY_USER}:${ARTIFACTORY_TOKEN}@artifactory.cnes.fr/artifactory/api/pypi/pypi/simple" > /tmp/artifactory_url
    - >
      podman build
      --network=host
      --build-arg IMAGE_SOURCE=${PUBLIC_REMOTE_REGISTRY_DOCKER}/
      --build-arg no_proxy=cnes.fr
      --file Dockerfile
      --tag "${DOCKER_IMAGE_NAME}:${VERSION_GRS}"
      --tag "${DOCKER_IMAGE_NAME}:latest"
      --secret id=http_proxy,src=/tmp/proxy_http
      --secret id=https_proxy,src=/tmp/proxy_https
      --secret id=artifactory_url,src=/tmp/artifactory_url
      --creds="${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD}"
      .
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web" || $CI_PIPELINE_SOURCE == "merge_request_event" || $CI_COMMIT_BRANCH'
  artifacts:
    untracked: true
    expire_in: 1 day

# =============================================================================
# STAGE : package — Build & Push
# =============================================================================
podman-build:
  stage: package
  image:
    name: ${PUBLIC_REMOTE_REGISTRY_DOCKER}/${IMAGE_PODMAN}
    entrypoint: [""]
  needs:
    - python-tests
  before_script:
    # - !reference [default, before_script]
    # - !reference [.jfrog_helpers, restore_config]
    - mkdir -p certs
    - cp ${CNES_CERTIFICATE} certs/ca-bundle.crt
    - cat ${CNES_CERTIFICATE} >> certs/ca-certificates.crt
    - echo "$HTTP_PROXY" > /tmp/proxy_http
    - echo "$HTTPS_PROXY" > /tmp/proxy_https
    - export VERSION_GRS=$(grep version pyproject.toml | cut -d '"' -f 2)
    - if [ -n "${CI_COMMIT_TAG}" ]; then export VERSION_GRS=${CI_COMMIT_TAG}; fi
    - echo "Version GRS -> ${VERSION_GRS}"
  script:
    - echo "https://${ARTIFACTORY_USER}:${ARTIFACTORY_TOKEN}@artifactory.cnes.fr/artifactory/api/pypi/pypi/simple" > /tmp/artifactory_url
    - >
      podman build
      --network=host
      --build-arg IMAGE_SOURCE=${PUBLIC_REMOTE_REGISTRY_DOCKER}/
      --build-arg no_proxy=cnes.fr
      --file Dockerfile
      --tag "${DOCKER_IMAGE_NAME}:${VERSION_GRS}"
      --tag "${DOCKER_IMAGE_NAME}:latest"
      --secret id=http_proxy,src=/tmp/proxy_http
      --secret id=https_proxy,src=/tmp/proxy_https
      --secret id=artifactory_url,src=/tmp/artifactory_url
      --creds="${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD}"
      .    
    - podman push --digestfile image-file-details --creds="${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD}" "${DOCKER_IMAGE_NAME}:${VERSION_GRS}"
    - podman push --creds="${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD}" "${DOCKER_IMAGE_NAME}:latest"
    # - IMAGE_SHA=$(cat image-file-details)
    # - echo "${DOCKER_IMAGE_NAME}:${VERSION_GRS}@${IMAGE_SHA}" > image-file-details
    # - cat image-file-details
    # - ./jfrog rt build-docker-create ${TARGET_REPO} --server-id ${JF_UUID} --image-file image-file-details --build-name ${JFROG_CLI_BUILD_NAME} --build-number ${JFROG_CLI_BUILD_NUMBER}
    # - ./jfrog rt bp --server-id ${JF_UUID} --build-url ${CI_PIPELINE_URL} ${JFROG_CLI_BUILD_NAME} ${JFROG_CLI_BUILD_NUMBER}

  only:
    - tags
  artifacts:
    untracked: true
    expire_in: 1 day

# =============================================================================
# STAGE : package — Validation
# =============================================================================
docker-test:
  stage: package
  allow_failure: true
  variables:
    SINGULARITY_CACHEDIR: "/local/${USER}/.singularity"
    SINGULARITY_TMPDIR:   "/local/${USER}/tmp"
  needs:
    - podman-build
  tags:
    - tpa
  before_script:
    - module load singularity/3.10.2
    - export SINGULARITY_CACHEDIR="${CI_PROJECT_DIR}/.singularity/cache"
    - export SINGULARITY_TMPDIR="${CI_PROJECT_DIR}/.singularity/tmp"
    - export JOB_UUID=$(cat /proc/sys/kernel/random/uuid)
    - mkdir -p "${SINGULARITY_CACHEDIR}" "${SINGULARITY_TMPDIR}"
    - singularity remote login --username "${ARTIFACTORY_USER}" --password "${ARTIFACTORY_TOKEN}" docker://artifactory.cnes.fr
    - singularity pull grs.sif docker://${DOCKER_IMAGE_NAME}:latest
    - export MY_JOB_WORKDIR="/work/scratch/data/${USER}/${JOB_UUID}"
    - mkdir -p ${MY_JOB_WORKDIR}/LOGS ${MY_JOB_WORKDIR}/grs2
    - rsync -av --exclude='.git' --exclude='.jfrog' --exclude='python_env' ./ ${MY_JOB_WORKDIR}/grs2/
    - ssh trex.sis.cnes.fr "mkdir -p ${MY_JOB_WORKDIR}/grs2 ${MY_JOB_WORKDIR}/LOGS"
    - rsync -av --exclude='.git' --exclude='.jfrog' --exclude='python_env' ./ trex.sis.cnes.fr:${MY_JOB_WORKDIR}/grs2/
  script:
    - |
      ssh trex.sis.cnes.fr "MY_JOB_WORKDIR='${MY_JOB_WORKDIR}' REF_DIR='${REF_DIR}' ARTIFACTORY_TOKEN='${ARTIFACTORY_TOKEN}' ARTIFACTORY_USER='${ARTIFACTORY_USER}' bash -s" << 'EOF'
        set -e
        sub_str=$(sbatch --wait --export=MY_JOB_WORKDIR=${MY_JOB_WORKDIR} -o ${MY_JOB_WORKDIR}/LOGS/grs-docker_test.out ${MY_JOB_WORKDIR}/grs2/grs/tests/ci/ci-docker-run.slurm)
        id_job=${sub_str##* }
        echo "Job tests -> ${id_job}"
        echo "=== RÉSULTAT DU JOB ==="
        cat "${MY_JOB_WORKDIR}/LOGS/grs-docker_test.out"
      EOF
    - mkdir -p reports
    - scp -r trex.sis.cnes.fr:${MY_JOB_WORKDIR}/reports/* reports/ || echo "Pas de dossier reports"
    - scp trex.sis.cnes.fr:${MY_JOB_WORKDIR}/LOGS/* reports/ || echo "Pas de logs"
    - cat reports/docker-report.xml
  only:
    - tags
  artifacts:
    untracked: true
    expire_in: 1 day